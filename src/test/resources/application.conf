h2mem {
  connectionPool = "HikariCP"
  url = "jdbc:h2:mem:tsql1;INIT=runscript from 'src/test/resources/create-schema.sql'"
  driver = org.h2.Driver
  numThreads = 2
  keepAliveConnection = true
  connectionTimeout = 1 seconds
}